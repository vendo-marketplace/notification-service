spring:
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:vendo.marketplace.real@gmail.com}
    from: ${MAIL_FROM:vendo.marketplace.real@gmail.com}
    password: ${MAIL_PASSWORD:qivlskruhrwqizxy}
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
  data:
    redis:
      port: ${REDIS_PORT:6379}
      host: ${REDIS_HOST:localhost}

eureka:
  client:
    enabled: false

redis:
  password-recovery:
    email:
      prefix: "test-email-prefix:"
      ttl: 600
    otp:
      prefix: "test-otp-prefix:"
      ttl: 600
  email-verification:
    email:
      prefix: "test-email-prefix:"
      ttl: 600
    otp:
      prefix: "test-otp-prefix:"
      ttl: 600


kafka:
  events:
    password-recovery-event:
      topic: "PASSWORD_RECOVERY_TEST_TOPIC"
      groupId: "password_recovery_test_group"
      properties:
        auto-offset-reset: "earliest"
    email-verification-event:
      topic: "EMAIL_VERIFICATION_TEST_TOPIC"
      groupId: "email_verification_test_group"
      properties:
        auto-offset-reset: "earliest"